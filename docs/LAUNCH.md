# Launch Architecture

The catalog contains 100 products/services across 10 categories. Each category exposes its 3 featured products as the primary merchandising layer.

Catalog source: `catalog/catalog.json`.

Storefront: `index.html`.

All product records are bilingual (DE/TR) and include a CTA. Checkout is intentionally provider-neutral until payment credentials and fulfillment rules are configured.
